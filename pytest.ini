[pytest]
testpaths = tests
pythonpath =
    .
    src
addopts = -ra
markers =
    slow: 大样本 CSV（如 5628 行），耗时较长
    e2e: Playwright 浏览器端到端（需前端可访问）
    perf: 集成与性能（并发分类、报表 SLA）
filterwarnings =
    ignore::DeprecationWarning
