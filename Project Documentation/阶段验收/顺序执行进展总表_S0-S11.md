# VOC 顺序执行进展总表（S0–S11）

> **本文件是进展唯一汇总入口**（状态、验收结论、离线包、下一步都写这里）。  
> 详细计划：`docs/plans/2026-09-13-sequential-execution-plan.md`  
> 专项长文仅作附录链接；日常只维护本表。  
> 分支：`feature/voc-l3-insight-rollout`  
> 约定：生产分类保持 **14B**；`VOC_L3_STANDARD_V1` / `VOC_INSIGHT_FIELDS_V1` **默认关闭**，未明示不开。

---

## 总览

| 步骤 | 状态 | 关键 commit | 专项附录（可选） |
|------|------|-------------|------------------|
| S0 分支与归档 | ✅ | 见计划监督表 | — |
| S1 周报工具链 | ✅ | `ea52a85` | — |
| S2 R29 复核 | ✅ | `3cafb66` | — |
| S3 214 L3 | ✅ | `b068b92` | — |
| S4 主题映射 | ✅ | `38c9ca5` | — |
| S5 L3 dry-run | ✅ | `2bbbe4d` | — |
| S6 洞察字段 | ✅ 开发机；人工 CSV 一致率待补 | `2792f78` | [s6-eval](../../docs/plans/2026-09-24-s6-insight-fields-eval.md) |
| S7 跨记录洞察 | ✅ 只读模块，不写库 | `0eda97e` | [s7-eval](../../docs/plans/2026-09-24-s7-cross-record-insights-eval.md) |
| S8 嵌入与跨 L2 | ✅ 开发机 + 桌面机 bge-m3 抽检≈80% | `ffb944b`…`6134106` | [S8 进展](./S8_嵌入模型与跨L2聚集进展.md) |
| S9 汇报下钻 | ✅ 能力达标；CSV/前10 抽核建议补完 | `53c556c`…`ca8f2e8` | [S9 进展](./S9_汇报三级下钻进展.md) |
| **S10 模型预评估** | **⬜ 下一步** | — | — |
| S11 新机迁移 | ⬜ 等硬件到货 | — | — |

---

## 分步结论摘要

### S0–S5（已关门）

| 步骤 | 结论 |
|------|------|
| S0 | 功能分支与计划归档就绪 |
| S1 | 周报工具链可用；离线包已交 |
| S2 | 复核三按钮浏览器验收通过 |
| S3 | 214 定位 L3 + 排除项业务确认 |
| S4 | 33 汇报主题映射业务确认 |
| S5 | 开发机/服务器 400 条 dry-run `gate_ok`；生产 flag 默认关 |

### S6 洞察字段

- 单测 7/7；200 条 dry-run `gate_ok`；分类核心字段不变。
- 生产默认关 `VOC_INSIGHT_FIELDS_V1`。
- 未完成：200 条人工一致率 CSV 标注（不阻塞 S7–S9）。

### S7 跨记录洞察

- 证据链对账 `gate_ok`；只读聚合，不写库。
- 服务器以离线脚本/包同步 `label_project` 使用，无独立「生产开关」。

### S8 嵌入与跨 L2 聚集

- 独立 `opinion_embedding.db`；hashing 降级 + Ollama `bge-m3`。
- 桌面机：`min_sim≈0.82` + mega-cluster 门禁；人工纯度抽检约 **80%**。
- 离线包扩展：zip 含 `label_project` / `performance_evaluation` / `update_server.sh`。

### S9 汇报下钻（客诉主题台账）— 2026-09-24 实机

**目标达成**：报告页「主题 → L3 → 脱敏原文」；环比/趋势；已复核 vs 仅模型；CSV 导出。

| 验收项 | 结果 |
|--------|------|
| API 单测 / 前端 build | ✅ |
| 主题条数 = CSV 行数（单测） | ✅ |
| VIN/手机脱敏（单测） | ✅ |
| 服务器浏览器：主题列表 | ✅ **1616 条 / 32 主题**（日期 2026-04-01～09-24） |
| 服务器浏览器：查看原文明细 | ✅ 用户确认可见 |
| 导出 CSV 实机抽 1–2 主题 | ⬜ 建议补 |
| 前 10 主题人工过一眼 | ⬜ 建议补（注意 T99「其他-待归类」占比偏高） |

**浏览器验收中修过的问题**：

1. 命名：「主题证据链」→ **客诉主题台账**；按钮「按日期查主题」。
2. 空列表误报「缺三级」：最新 3000 条无 L3 挤掉历史已分类 → **优先取带 L3 行**（`ca8f2e8`）。
3. 物化列空时回退 `v3_label_meta`（`ebc6158`）。

**当前生效离线包（S9fix3）**：

| 项 | 值 |
|----|-----|
| commit | `ca8f2e8` |
| 归档 | `VOC_S9fix3_ca8f2e8_20260924_160718.zip` |
| MD5 | `93a1166efe3d680a8aa66b47855cb825` |
| 传递 | `code_deploy/update.zip` + `update.zip.md5` 成对 |

**界面观察（非故障）**：人工已复核多为 0（仅模型）；T99 量大属映射未命中，非台账坏掉。

---

## 下一步（严格顺序）

### 立即：S10 — 模型角色拆分与旧机 32B 离线预评估

- 分类生产继续 **14B**，不写 32B 结果进生产库。
- 增加汇报/仲裁模型配置位（如 `VOC_QWEN_MODEL_REPORT` / `ARBITER`）。
- 非生产时段小样本 + 固定 holdout A/B；OOM 则记录并等新机。
- 完成物：S10 commit + A/B 报告 + 本表更新。

### 其后：S11 — 新服务器到货后迁移

1. 新机部署当前已推送分支，先 14B 一周基线  
2. 汇报模型升级，分类仍 14B  
3. A/B 过门禁再考虑分类升 32B  
4. 最后才开低置信仲裁  

### S9 收尾（可与 S10 并行、不阻塞开题）

- [ ] 抽主题导出 CSV：行数对账 + 脱敏眼检  
- [ ] 前 10 主题人工抽核（含 T99 是否可接受）

---

## 更新日志

| 日期 | 说明 |
|------|------|
| 2026-09-24 | 建立本总表；S8 开发机嵌入/聚类（hashing） |
| 2026-09-24 | 补链 S6/S7；S8 桌面机 bge-m3 抽检≈80% |
| 2026-09-24 | S9 开发机 API/UI；离线包多轮；浏览器列表空 → 修取样 |
| 2026-09-24 | **S9 实机通过**：32 主题 + 明细可见；本表升格为唯一汇总；**下一步 S10** |
